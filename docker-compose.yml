version: '3.8'

services:
  mcp-agent-fabric:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3008:3008"
    environment:
      - PORT=3008
      - NODE_ENV=production
    restart: unless-stopped
